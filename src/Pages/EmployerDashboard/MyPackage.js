import React, { useContext, useEffect, useState } from 'react';
import { getPostNumber } from '../../api/pack';
import { AuthContext } from '../../contexts/AuthProvider';

const MyPackage = () => {
    
    return (
        <div>
            <p>My Package</p>
            
        </div>
    );
};

export default MyPackage;